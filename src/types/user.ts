export interface ApiUser {
    id: number | string
    first_name: string
    last_name: string
    avatar: string
    email: string
}

export interface User {
    id: number
    firstName: string
    lastName: string
    avatar: string
    email: string
}
